apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    {{- include "resultchart.labels" . | nindent 4 }}
  name: {{ .Values.result.service.name | quote }}
  namespace: {{ .Values.result.service.namespace | quote}}
spec:
  replicas: {{ .Values.replicaCount}}
  selector:
    matchLabels:
      {{- include "resultchart.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "resultchart.labels" . | nindent 8 }}
    spec:
      containers:
      - image: {{ .Values.result.image.repository}}
        name: {{ .Values.result.containers.name }}
        ports:
        - containerPort: {{ .Values.result.service.targetport }}
          name: {{ .Values.result.containers.name }}
